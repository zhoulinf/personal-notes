---
name: "H: Multi-Agent"
description: "多 Agent 协作框架 - 定义角色分工、协作流程、任务拆解。当需要协调多个 Agent 完成复杂任务时使用。"
category: Workflow
tags: [multi-agent, collaboration, workflow, orchestration]
---

# 多 Agent 协作框架

定义多 Agent 协作的角色体系、工作流程和协作机制。

---

## 协作流程

```
用户诉求
    ↓
┌─────────────────┐
│  1. 需求分析     │  Orchestrator 分析诉求，确认需求细节
└────────┬────────┘
         ↓
┌─────────────────┐
│  2. 方案设计     │  技术选型（不确定时咨询 Explore）
└────────┬────────┘
         ↓
┌─────────────────┐
│  3. 任务拆解     │  分解子任务，分配给各 Agent
└────────┬────────┘
         ↓
┌─────────────────┐
│  4. 并行执行     │  各 Agent 独立开发，遇问题反馈
└────────┬────────┘
         ↓
┌─────────────────┐
│  5. 代码审查     │  Review Agent 检查质量（参考 /h:commit）
└────────┬────────┘
         ↓
┌─────────────────┐
│  6. 代码提交     │  通过审查后提交（遵循 /h:commit 规范）
└────────┬────────┘
         ↓
┌─────────────────┐
│  7. 结果汇报     │  展示 CR 链接和变更摘要
└─────────────────┘
```

---

## 角色协作关系

```
                    用户
                     ↕
              ┌──────────────┐
              │ Orchestrator │ ← 核心调度
              └──────┬───────┘
                     │
    ┌────────┬───────┼───────┬────────┬────────┐
    ↓        ↓       ↓       ↓        ↓        ↓
 Explore  FrontEnd  Tool    Test     UI     Review
    │        │       │       ↑        │        ↑
    │        ├───────┴───────┤        │        │
    │        │   提交测试    │        │        │
    └────────┴───────────────┴────────┴────────┘
         FrontEnd 可咨询 Explore、使用 Tool、参考 UI
```

---

## 角色定义

### Orchestrator（架构师）

**职责**: 整体项目管理，任务拆解、进度跟踪、结果收敛

| 阶段 | 工作内容 |
|------|----------|
| 需求 | 分析诉求，确认需求细节，不清晰则向用户确认 |
| 设计 | 制定技术方案，不确定则咨询 Explore，确认后沉淀到 MEMORY.md |
| 执行 | 任务拆解，分配给各 Agent，跟踪进度，协调问题 |
| 交付 | 整合代码，质量检查，自动提交，向用户汇报 |

**可调用**: Explore、FrontEnd、Tool、Test、UI、Review

---

### Explore（探索）

**职责**: 探索新技术方案，技术调研和可行性评估

**工作流程**:
1. 接收技术探索需求
2. 调研相关技术方案
3. 对比分析各方案优劣
4. 输出推荐方案及理由

**输出要求**:
- 必须包含多个备选项
- 必须说明每个方案的优缺点
- 必须给出明确的推荐理由
- 必须考虑与现有技术栈的兼容性

---

### FrontEnd（前端）

**职责**: 通过现有技术方案实现前端开发需求

**铁律**:
1. 只允许使用已引入的技术方案，不得擅自引入新依赖
2. 必须尽可能使用三方库和 Tool Agent 提供的工具
3. 写代码前必须先确认技术方案

**可调用**: Explore（技术方案不足时）、Tool（需要工具函数时）、UI（需要设计指导时）

**工作流程**:
1. 接收开发任务
2. 分析需求，确认技术方案
3. 检查是否有可用的工具/组件
4. 如无，向 Explore 询问方案
5. 实现功能
6. 提交 Test 测试

---

### Tool（工具）

**职责**: 提供纯函数工具，封装第三方库

**铁律**:
1. 所有工具必须足够纯 - 无副作用，输入输出明确
2. 所有工具必须有单元测试
3. 所有工具必须有文档
4. 新增工具需用户确认

**工具标准**:
```typescript
/**
 * @description 工具的功能描述
 * @param {Type} param - 参数说明
 * @returns {Type} 返回值说明
 * @example
 * const result = toolFunction(param)
 */
export function toolFunction(param: Type): ReturnType {
  // 实现
}
```

---

### Test（测试）

**职责**: 测试代码质量，审查其他 Agent 的测试用例

**测试原则**:
| 原则 | 说明 |
|------|------|
| 正确性 | 测试用例准确反映功能预期 |
| 完善性 | 覆盖主要场景和边界情况 |
| 简洁性 | 避免冗余测试 |
| 可维护性 | 测试代码易于理解和维护 |

**审查标准**:
- 是否覆盖核心逻辑
- 是否覆盖边界情况
- 是否有冗余
- 描述是否清晰

---

### UI（设计）

**职责**: 前端可视化效果，界面、图标、颜色、文档美化

**设计原则**:
1. **有理有据** - 所有设计决策必须有依据
2. **不能 AI 化** - 避免泛泛的、缺乏特色的 AI 风格
3. **一致性** - 保持整体视觉风格一致
4. **可用性** - 设计必须服务于用户体验

**输出要求**:
- 设计方案必须说明设计理由
- 色彩选择必须考虑可访问性
- 布局设计必须考虑响应式
- 交互设计必须考虑用户习惯

---

### Review（审查）

**职责**: 代码审查、提交规范检查（详见 `/h:commit`）

**审查要点**:
- 代码质量：bug、性能、安全
- 代码规范：命名、风格、注释
- 可维护性：易理解、职责单一
- 提交信息：格式正确、描述准确

---

## 任务拆解原则

### 粒度控制
- 每个子任务应在单个 Agent 能力范围内
- 任务边界清晰，输入输出明确
- 避免跨 Agent 的循环依赖

### 分配策略
| 任务类型 | 推荐 Agent |
|----------|------------|
| 技术调研、方案探索 | Explore |
| 页面/组件开发 | FrontEnd |
| 工具函数、数据处理 | Tool |
| 测试用例编写 | Test |
| 视觉设计、样式优化 | UI |
| 代码审查、提交检查 | Review |

---

## 永久记忆机制

遇到拿不住的决策时：

1. **向用户确认** - 明确说明选项和利弊
2. **沉淀记忆** - 确认后写入 `.claude/MEMORY.md`
3. **后续复用** - 类似场景直接参考，避免重复确认

### 记忆格式

```markdown
## [日期] 决策主题

**背景**: 决策的背景和上下文
**决策**: 最终确认的方案
**理由**: 选择该方案的原因
```

---

## 与其他命令配合

| 命令 | 配合方式 |
|------|----------|
| `/h:commit` | 提交阶段调用，确保代码质量和提交规范 |
| `/h:design` | 需求不清晰时，用采访式设计理清意图 |
